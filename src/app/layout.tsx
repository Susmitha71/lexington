import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Lexington Capital - Secure Business Funding Solutions",
  description: "Leading business purpose lender offering SBA loans, merchant cash advances, lines of credit, and more. Over $50M+ in loan origination. Get the capital you need to grow your business today.",
  keywords: "business loans, SBA loans, merchant cash advance, business funding, Lexington Capital",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
